apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP
  ports:
    - name: "backend-port"
      protocol: "TCP"
      port: 5050
      targetPort: 5050
  selector:
    app: backend
